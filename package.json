{
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon index.ts",
    "start": "cross-env NODE_ENV=production pm2 start index.js --name go-cqhttp-node --log-date-format 'YYYY-MM-DD HH:mm:ss'",
    "reload": "pm2 reload go-cqhttp-node",
    "send": "cross-env NODE_ENV=development nodemon send_file/index.ts",
    "test": "cross-env NODE_ENV=development nodemon test.ts"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "ws": "^7.4.2"
  },
  "license": "MIT",
  "devDependencies": {
    "@antfu/eslint-config": "^0.30.1",
    "@types/node": "^18.11.9",
    "@types/ws": "^8.5.3",
    "cross-env": "^7.0.3",
    "eslint": "^8.27.0",
    "nodemon": "^2.0.7",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "eslintConfig": {
    "extends": "@antfu"
  }
}
